# Backlog

No outstanding release-blocking items.

The pre-handoff review findings (B-43..B-49, B-51) have all landed on
`feature/release-1.0-prep`. B-50 was rescoped: the PRD it referenced is
slated for deletion, so reconciling its acceptance-criteria copy added
no value.

Durable architectural context lives in `docs/ARCHITECTURE.md`; this file
remains for any new pre-1.0 work that surfaces.
